package com.tecnova.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Esta clase no necesita más código, Springdoc OpenAPI lo gestiona automáticamente
}
